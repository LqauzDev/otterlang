dependency:
  name: "chrono"
  version: "0.4"
  features: ["clock"]
functions:
  - name: "now_rfc3339"
    result: "Str"
    doc: "Returns the current UTC timestamp formatted as RFC 3339."
    call:
      kind: expr
      expr: "ffi_dep::Utc::now().to_rfc3339()"
