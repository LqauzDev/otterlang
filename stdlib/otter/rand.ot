fn seed(n: int):
    rand.seed(n)

fn int(min: int, max: int) -> int:
    return rand.int(min, max)

fn float() -> float:
    return rand.float()

fn bytes(n: int) -> string:
    return rand.bytes(n)

fn uuid() -> string:
    return rand.uuid()
